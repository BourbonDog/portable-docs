## Slide One

A fenced example that contains a slide separator and a marker — neither the
`---` nor the `@chart` inside this fence should affect slide splitting:

```markdown
---
<!-- @chart type="bar" -->
```

---

## Slide Two

Real content after a real `---` separator.
